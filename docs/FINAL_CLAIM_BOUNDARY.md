# Final Claim Boundary - v1.0.0

## Derived in the retained heavy-q branch

- the scalar `q` already exists in the frozen SFV/dSB action;
- the true-vacuum mass is `m_q = 8.9804766577e14 GeV`;
- the frozen EFT has exact `q -> -q` parity, stabilizing the lightest q-odd excitation;
- after production, the massive relic redshifts as cold matter;
- resolved handoff-band modes are already nonrelativistic and have negligible late free streaming;
- shot noise, late annihilation, and astrophysical self-interaction are negligible in the conditional abundance regime;
- the existing `g P^2 q^2` portal supplies pair-production channels, including `M(pi pi -> q q; s) = 4 g s / (s - m_sigma^2)` up to an overall sign;
- direct `sigma -> q q` decay is kinematically forbidden.

## Conditional

Exactly one new universal dark history/state normalization is retained:

`Y_q = n_q / s`.

The reference `Omega_c h^2 = 0.1200` corresponds to

`Y_q = 4.8701384772e-25`.

This value is a holdout-inferred conditional abundance normalization, not a first-principles production prediction.

## Not used as inputs

- historical H8 numerical amplitudes;
- historical `Lambda_mem`;
- galaxy/RAR fitting data;
- per-galaxy dark parameters;
- a new dark scalar, dark mass, or dark production coupling.

## First-principles stop

The absolute yield cannot yet be derived because the frozen `2+1 -> 3+1` handoff does not uniquely determine:

1. channel-resolved energy loading;
2. the post-handoff Phi-visible thermal connector/temperature relation;
3. the threshold-complete Pati-Salam breaking spectrum and thermal degrees of freedom.

No further UV structure is added inside the dark-matter project solely to repair the abundance target.
